<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/src/main/java/com/taskflow/backend/security/JwtAuthenticationFilter.java">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/src/main/java/com/taskflow/backend/security/JwtAuthenticationFilter.java" />
              <option name="originalContent" value="package com.taskflow.backend.security;&#10;&#10;import com.taskflow.backend.service.JwtService;&#10;import jakarta.servlet.FilterChain;&#10;import jakarta.servlet.ServletException;&#10;import jakarta.servlet.http.HttpServletRequest;&#10;import jakarta.servlet.http.HttpServletResponse;&#10;import java.io.IOException;&#10;import lombok.RequiredArgsConstructor;&#10;import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;&#10;import org.springframework.security.core.context.SecurityContextHolder;&#10;import org.springframework.security.core.userdetails.UserDetails;&#10;import org.springframework.security.core.userdetails.UserDetailsService;&#10;import org.springframework.security.web.authentication.WebAuthenticationDetailsSource;&#10;import org.springframework.stereotype.Component;&#10;import org.springframework.web.filter.OncePerRequestFilter;&#10;&#10;@Component&#10;@RequiredArgsConstructor&#10;public class JwtAuthenticationFilter extends OncePerRequestFilter {&#10;&#10;    private final JwtService jwtService;&#10;    private final UserDetailsService userDetailsService;&#10;&#10;    @Override&#10;    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response, FilterChain filterChain)&#10;            throws ServletException, IOException {&#10;        String authHeader = request.getHeader(&quot;Authorization&quot;);&#10;        if (authHeader == null || !authHeader.startsWith(&quot;Bearer &quot;)) {&#10;            filterChain.doFilter(request, response);&#10;            return;&#10;        }&#10;&#10;        String token = authHeader.substring(7);&#10;        String username;&#10;        try {&#10;            username = jwtService.extractUsername(token);&#10;        } catch (Exception ex) {&#10;            filterChain.doFilter(request, response);&#10;            return;&#10;        }&#10;&#10;        if (username != null &amp;&amp; SecurityContextHolder.getContext().getAuthentication() == null) {&#10;            UserDetails userDetails = userDetailsService.loadUserByUsername(username);&#10;            if (jwtService.isTokenValid(token, userDetails)) {&#10;                UsernamePasswordAuthenticationToken authToken = new UsernamePasswordAuthenticationToken(&#10;                        userDetails,&#10;                        null,&#10;                        userDetails.getAuthorities()&#10;                );&#10;                authToken.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));&#10;                SecurityContextHolder.getContext().setAuthentication(authToken);&#10;            }&#10;        }&#10;&#10;        filterChain.doFilter(request, response);&#10;    }&#10;}&#10;&#10;" />
              <option name="updatedContent" value="package com.taskflow.backend.security;&#10;&#10;import com.taskflow.backend.service.JwtService;&#10;import jakarta.servlet.FilterChain;&#10;import jakarta.servlet.ServletException;&#10;import jakarta.servlet.http.HttpServletRequest;&#10;import jakarta.servlet.http.HttpServletResponse;&#10;import java.io.IOException;&#10;import lombok.RequiredArgsConstructor;&#10;import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;&#10;import org.springframework.security.core.context.SecurityContextHolder;&#10;import org.springframework.security.core.userdetails.UserDetails;&#10;import org.springframework.security.core.userdetails.UserDetailsService;&#10;import org.springframework.security.web.authentication.WebAuthenticationDetailsSource;&#10;import org.springframework.stereotype.Component;&#10;import org.springframework.web.filter.OncePerRequestFilter;&#10;&#10;@Component&#10;@RequiredArgsConstructor&#10;public class JwtAuthenticationFilter extends OncePerRequestFilter {&#10;&#10;    private final JwtService jwtService;&#10;    private final UserDetailsService userDetailsService;&#10;&#10;    @Override&#10;    protected boolean shouldNotFilter(HttpServletRequest request) {&#10;        String path = request.getServletPath();&#10;        return path != null &amp;&amp; path.startsWith(&quot;/api/auth&quot;);&#10;    }&#10;&#10;    @Override&#10;    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response, FilterChain filterChain)&#10;            throws ServletException, IOException {&#10;        String authHeader = request.getHeader(&quot;Authorization&quot;);&#10;        if (authHeader == null || !authHeader.startsWith(&quot;Bearer &quot;)) {&#10;            filterChain.doFilter(request, response);&#10;            return;&#10;        }&#10;&#10;        String token = authHeader.substring(7);&#10;        String username;&#10;        try {&#10;            username = jwtService.extractUsername(token);&#10;        } catch (Exception ex) {&#10;            filterChain.doFilter(request, response);&#10;            return;&#10;        }&#10;&#10;        if (username != null &amp;&amp; SecurityContextHolder.getContext().getAuthentication() == null) {&#10;            UserDetails userDetails = userDetailsService.loadUserByUsername(username);&#10;            if (jwtService.isTokenValid(token, userDetails)) {&#10;                UsernamePasswordAuthenticationToken authToken = new UsernamePasswordAuthenticationToken(&#10;                        userDetails,&#10;                        null,&#10;                        userDetails.getAuthorities()&#10;                );&#10;                authToken.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));&#10;                SecurityContextHolder.getContext().setAuthentication(authToken);&#10;            }&#10;        }&#10;&#10;        filterChain.doFilter(request, response);&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>